import { spacing } from "@/constants";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  form: {
    gap: spacing.space16.height,
  },
  submitButton: {
    marginTop: spacing.space40.height,
  },
});

export default styles;
